---
name: Build Workflow

on:
  workflow_call:

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: ⬇ Checkout repo
        uses: actions/checkout@v3

      - name: 🔄 Init Cache
        uses: ./.github/actions/npm-cache

      - name: Decode base64 secrets
        run: |
          touch .env
          echo $VITE_FIREBASE_API_KEY | base64 -d >> .env
          echo $VITE_FIREBASE_AUTH_DOMAIN | base64 -d >> .env
          echo $VITE_FIREBASE_PROJECT_ID | base64 -d >> .env
          echo $VITE_FIREBASE_STORAGE_BUCKET | base64 -d >> .env
          echo $VITE_FIREBASE_MESSAGING_SENDER_ID | base64 -d >> .env
          echo $VITE_FIREBASE_APP_ID | base64 -d >> .env
        env:
          VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}

      - name: 🔨 Build
        run: npm run build

      - name: ⬆ Upload build
        uses: ./.github/actions/upload-tar-artifact
        with:
          name: build-output
          path: build
